# This repo contains configuration files to make work on new hardware much easier

## Brew

```bash
# Install all packages from Brewfile
brew bundle
# Create Brewfile with all installed packages
brew bundle dump
```
